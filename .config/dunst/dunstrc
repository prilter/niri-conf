[global]
    monitor = 0                          # Show on primary monitor
    follow = mouse                       # Notifications follow mouse
    width = 200                          # Notification width
    offset = "30x50"                     # Position from top-right
    origin = top-right                   # Anchor position
    alignment = center                   # Text alignment
    corner_radius = 8                    # Rounded corners
    frame_width = 3                      # Border thickness
    gap_size = 3                         # Space between notifications

    # Fonts
    font = "Ubuntu Mono 13"       # Primary font
    markup = no                         # Enable HTML-like markup
    format = "<b>%s</b>\n%b"             # Bold title, newline for body

    # Colors (Nord Theme example)
    background = "#262626"               # background
    foreground = "#FFAF5F"               # text
    frame_color = "#ffffff"              # Border color
